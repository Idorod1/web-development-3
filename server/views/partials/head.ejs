<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="site-header">
    <a class="brand" href="/">HTTP Cookbook</a>
    <nav class="site-nav">
      <a href="/" class="<%= active === 'game' ? 'is-active' : '' %>">Game</a>
      <a href="/schemas" class="<%= active === 'schemas' ? 'is-active' : '' %>">Schemas</a>
    </nav>
  </header>
